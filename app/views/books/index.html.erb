<%= render 'books/new', books: @books %>

<h2>Books</h2>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
<h1>New book</h1>

 <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
 <% end %>

 <%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :opinion %>
  <%= f.submit 'Create Book' %>
 <% end %>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><a href=/users/id/>
          <img class="attachment user profile_image fallback"></a>
        </td>
        <td><a class="book_id" href="/books/id"></a>
        </tb>
      </tr>
    <% end %>
  </tbody>
</table>

<footer>
  <p>CopyRight Infratop.inc</p>
</footer>